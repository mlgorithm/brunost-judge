This is a public smoke-test task, not a contest task. Submit a non-empty file
named `answer.txt` to receive full score. It intentionally contains no hidden
answer or production-quality scoring rule.

For a real IOAI task, keep labels and answer keys in `private/` outside the
public source repository, and test wrong, missing, and malformed submissions.
