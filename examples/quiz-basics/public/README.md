# Judge basics quiz

This is a minimal `quiz` package. Publish any participant-facing question text
and instructions here, but keep the answer key only in `private/questions.json`.

A matching submission contains one `answers.json` file:

```json
{"answers":{"language":"python","immutable":"immutable"}}
```
