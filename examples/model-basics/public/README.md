# Model basics

This package demonstrates the smallest complete `train_predict_v2` task. Copy
`submission.example.py` to a submission bundle as `submission.py`; the Judge
trains once and predicts against the private test split before the evaluator
scores it.
